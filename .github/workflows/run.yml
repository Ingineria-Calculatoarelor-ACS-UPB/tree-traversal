name: 🚀 Run

on:
  push:
    branches:    
      - main

jobs:
  run:
    name: 🚀 Run
    runs-on: ubuntu-latest
    steps:
      - name: 🚀 Run
        shell: bash
        run: |
            doxygen -g
            doxygen
