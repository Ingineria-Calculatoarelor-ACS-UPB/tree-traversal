name: 🚀 Run

on:
  pull_request:
    branches:
      - main

jobs:
  run:
    name: 🚀 Run
    runs-on: ubuntu-latest
    steps:
      - name: 🚀 Run
        shell: bash
        run: |
          python -m pip install --upgrade pip
          python3 -m unittest tree.py
      - name: Discord notification
        if: ${{ failure() }}
        run: |
          curl -i -H "Accept: application/json" -H "Content-Type:application/json" -X POST --data "{\"content\": \"Posted Via Command line\"}" https://discord.com/api/webhooks/1103323884943581234/F6QvICcgtC3XSgEZyqehex1xJNNRtbqtISJ3OfgIjKbHpXYslkhcfAWhCQsmwZmcCk-I
