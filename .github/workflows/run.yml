name: 🚀 Run

on:
  push:
    branches:    
      - main

jobs:
  run:
    name: 🚀 Run
    runs-on: ubuntu-latest
    steps:
      - name: 🚀 Run
        shell: bash
        run: |
            sudo apt install doxygen
            doxygen -g
            doxygen
      - name: Upload a Build Artifact
        with:
            name: docfiles
            path: |
              html/
              latex/
              Doxyfile
            if-no-files-found: error

