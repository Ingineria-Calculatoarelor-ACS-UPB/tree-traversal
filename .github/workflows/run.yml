name: 🚀 Run

on:
  pull_request:
    branches:
      - main

jobs:
  run:
    name: 🚀 Run
    runs-on: ubuntu-latest
    steps:
      - name: 🚀 Run
        shell: bash
        run: |
          python3 -m unittest tree.py
