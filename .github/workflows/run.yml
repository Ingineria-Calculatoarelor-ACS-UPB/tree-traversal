name: 🚀 Run

on:
  workflow_dispatch:

jobs:
  run:
    name: 🚀 Run
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: 🚀 Run
        shell: bash
        run: |
          python3 main.py
  run2:
    name: 🚀 Run 2
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout 2
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: 🚀 Run 2
        shell: bash
        run: |
          python3 script.py
